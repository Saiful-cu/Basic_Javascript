function main() {
    const name = "Alice";
    const age = 22;
    const city = "Berlin";
    const formattedOutput = `${name} (age ${age}) lives in ${city}.`;
    console.log(formattedOutput);
}

main();
